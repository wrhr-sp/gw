{
  "name": "gw",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.5.2",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "check": "pnpm test && pnpm typecheck",
    "db:pg:check": "node scripts/validate-operational-postgres-schema.mjs",
    "db:pg:seed:preview": "node scripts/seed-operational-db.mjs preview",
    "db:pg:seed:production": "node scripts/seed-operational-db.mjs production"
  },
  "devDependencies": {
    "punycode": "^2.3.1"
  }
}
