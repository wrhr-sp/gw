{
  "name": "@werehere/file-processor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "scan:preview": "tsx --tsconfig tsconfig.runtime.json src/batch-cli.ts",
    "self-test:clamav": "tsx src/clamav-self-test-cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "sharp": "0.35.0"
  }
}
